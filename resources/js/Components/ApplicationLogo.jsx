// The Epic Craftings logo (public/brand/logo.png — the real artwork).
export default function ApplicationLogo({ className = '' }) {
    return <img src="/brand/logo.png" alt="Epic Craftings" className={className} />;
}
